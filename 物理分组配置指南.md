# 🔧 物理分组配置指南

## 问题解决方案

子弹与玩家自身碰撞的问题通过**物理分组系统**解决，确保：
- ✅ 玩家攻击只与敌人碰撞
- ❌ 玩家攻击不与玩家自身碰撞
- ❌ 相同类型对象不会互相碰撞

## 物理分组定义

| 分组ID | 分组名称 | 用途 | 对象类型 |
|--------|----------|------|----------|
| 1 | PLAYER | 玩家本体 | Player节点 |
| 2 | PLAYER_ATTACK | 玩家攻击 | 子弹、激光、火球等 |
| 4 | ENEMY | 敌人本体 | Enemy节点 |
| 8 | ENEMY_ATTACK | 敌人攻击 | 敌人技能（预留） |
| 16 | ENVIRONMENT | 环境对象 | 墙壁、障碍物 |

## 碰撞矩阵配置

### ⚠️ 重要：需要在Cocos Creator编辑器中手动配置

1. **打开项目设置**
   - 菜单：`项目 → 项目设置 → 物理`
   - 找到 `碰撞矩阵` 部分

2. **设置碰撞关系**

| 分组1 ↓ | 分组2 → | PLAYER(1) | PLAYER_ATTACK(2) | ENEMY(4) | ENEMY_ATTACK(8) | ENVIRONMENT(16) |
|---------|---------|-----------|------------------|----------|-----------------|-----------------|
| **PLAYER(1)** | ❌ | ❌ | ✅ | ✅ | ✅ |
| **PLAYER_ATTACK(2)** | ❌ | ❌ | ✅ | ❌ | ✅ |
| **ENEMY(4)** | ✅ | ✅ | ❌ | ❌ | ✅ |
| **ENEMY_ATTACK(8)** | ✅ | ❌ | ❌ | ❌ | ✅ |
| **ENVIRONMENT(16)** | ✅ | ✅ | ✅ | ✅ | ❌ |

### 碰撞矩阵说明
- ✅ = 发生碰撞
- ❌ = 不碰撞

**关键设置：**
- `PLAYER_ATTACK(2)` ↔ `PLAYER(1)` = ❌ （**解决子弹碰到玩家的问题**）
- `PLAYER_ATTACK(2)` ↔ `ENEMY(4)` = ✅ （玩家攻击能打到敌人）
- `ENEMY(4)` ↔ `PLAYER(1)` = ✅ （敌人能碰到玩家）

## 代码实现状态

### ✅ 已自动配置的分组

1. **玩家节点** → `PLAYER(1)`
   - 位置：`PlayerController.onLoad()`
   - 自动调用：`PhysicsGroupsSimple.configurePlayerPhysics()`

2. **玩家攻击** → `PLAYER_ATTACK(2)`
   - 子弹：`SkillManager.createBulletAttack()`
   - 其他技能：`SkillManager.createSkillAttackWithConfig()`
   - 自动调用：`PhysicsGroupsSimple.configurePlayerAttackPhysics()`

3. **敌人节点** → `ENEMY(4)`
   - 位置：`GameManager.spawnEnemy()`
   - 自动调用：`PhysicsGroupsSimple.configureEnemyPhysics()`

### 🔍 验证分组设置

在游戏运行时，控制台会显示：
```
🔧 设置玩家物理分组: PLAYER(1)
🔧 设置玩家攻击物理分组: PLAYER_ATTACK(2)
🔧 设置敌人物理分组: ENEMY(4)
```

## 📋 手动配置步骤

### 步骤1：检查现有预制体

确保所有攻击预制体都有以下组件：
- `RigidBody2D` ✅
- `Collider2D` (BoxCollider2D/CircleCollider2D) ✅

### 步骤2：验证物理分组

在场景中运行游戏，检查控制台日志：
- 看到分组设置日志 = ✅ 正常
- 子弹仍与玩家碰撞 = ❌ 需要设置碰撞矩阵

### 步骤3：配置碰撞矩阵（重要）

1. **打开项目设置**
   ```
   菜单栏 → 项目 → 项目设置 → 物理系统 → 碰撞矩阵
   ```

2. **设置分组**
   - 确保有5个分组：DEFAULT(0), PLAYER(1), PLAYER_ATTACK(2), ENEMY(4), ENVIRONMENT(16)
   - 如果没有，点击"+"添加分组

3. **配置碰撞关系**
   - 取消勾选：`PLAYER_ATTACK` ↔ `PLAYER`
   - 保持勾选：`PLAYER_ATTACK` ↔ `ENEMY`
   - 保持勾选：`ENEMY` ↔ `PLAYER`

## 🧪 测试验证

### 测试子弹碰撞
1. 启动游戏
2. 按空格发射子弹
3. **期望结果**：
   - ❌ 子弹不会在玩家位置停下
   - ✅ 子弹正常飞向敌人
   - ✅ 子弹击中敌人时销毁

### 调试信息
```javascript
// 在控制台运行，查看当前分组设置
PhysicsGroupsSimple.printCollisionMatrix();
```

## 🔧 故障排除

### 问题1：子弹仍然碰撞玩家
**原因**：碰撞矩阵未正确配置
**解决**：检查项目设置中的碰撞矩阵

### 问题2：子弹不能击中敌人
**原因**：取消了错误的碰撞关系
**解决**：确保 `PLAYER_ATTACK` 与 `ENEMY` 勾选

### 问题3：物理分组设置无效
**原因**：RigidBody2D或Collider2D组件缺失
**解决**：运行 `ComponentFixer` 自动修复

## 📊 分组架构图

```
游戏对象分组架构：

Player(1) ──✅碰撞──→ Enemy(4)
    ↑                    ↑
    ❌                   ✅
    ↓                    ↓
PlayerAttack(2) ──✅击中──→ Enemy(4)

关键点：
❌ Player ↔ PlayerAttack = 不碰撞
✅ PlayerAttack ↔ Enemy = 碰撞
✅ Player ↔ Enemy = 碰撞
```

## 🎯 最终效果

配置完成后：
- 🎮 玩家可以正常移动
- 🔫 子弹从玩家位置正常发射，不会立即停止
- 🎯 子弹正确击中敌人并造成伤害
- 👾 敌人可以碰撞玩家（如果需要接触伤害）
- 🌟 所有攻击类型（激光、火球、飞盘等）都正常工作 